[project]
name = "sbu-syllabus-mcp"
version = "0.1.0"
description = "Syllabus parser for Stony Brook University using MCP"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.26.0",
]
